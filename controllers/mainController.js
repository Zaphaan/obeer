const controller = {
    index(request, response) {
        response.render('index');
    },

    blonde(request, response) {
        response.render('index');
    },

    blanche(request, response) {
        response.render('index');
    },

    ambre(request, response) {
        response.render('index');
    },

    brune(request, response) {
        response.render('index');
    },

    ipa(request, response) {
        response.render('index');
    },
}

module.exports = controller;